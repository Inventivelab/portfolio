<div class="album text-muted">
  <div class="container">
    <%= source_helper("alert alert-warning") %>
    <div class="row <%= "sortable" if logged_in?(:contributor) %>">
      <%= render partial: @portfolios %>
      <%#= render partial: @portfolios, spacer_template: 'portfolio_ruler'  %>
    </div>
  </div>
</div>
    
