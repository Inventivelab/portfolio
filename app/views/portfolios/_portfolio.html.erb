<div class="card" data-id="<%= portfolio.id %>">
  <%= image_tag portfolio_img(portfolio.thumb_image, 'thumb') %>
  <p class="card-text"><span><%= link_to portfolio.title, portfolio_show_path(portfolio) %></span>
  </p>
</div>


<%#= link_to "Edit", edit_portfolio_path(portfolio) if logged_in?(:site_admin) %>
<%#= link_to 'Delete', portfolio, method: :delete, data: { confirm: 'Are you sure?' } if logged_in?(:site_admin) %>
